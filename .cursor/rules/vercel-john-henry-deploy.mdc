---
description: Deploy John Henry Oficial solo a prj_IFp21rJxIMfODIMqnBxmgP6EO7ZT
alwaysApply: true
---

# Vercel: John Henry Oficial (producción fija)

**projectId:** `prj_IFp21rJxIMfODIMqnBxmgP6EO7ZT`  
**projectName:** `john-henry-oficial`  
**orgId:** `team_ynCZ0ZoRGYzwY9nQlqA6e6rN`  
**URL producción:** https://john-henry-oficial.vercel.app

## Validación obligatoria antes de cualquier deploy

1. Leer `.vercel/project.json`.
2. Confirmar que `projectId` es exactamente `prj_IFp21rJxIMfODIMqnBxmgP6EO7ZT`.
3. Si falta `.vercel/project.json`, el `projectId` no coincide, o el inspect del proyecto falla → **cancelar** el deploy. No crear otro proyecto ni enlazar otro.

```bash
# Comprobar enlace local
cat .vercel/project.json
# Debe contener: "projectId": "prj_IFp21rJxIMfODIMqnBxmgP6EO7ZT"
```

## Deploy

Usar siempre `npx vercel` (nunca el binario global):

```bash
npx vercel deploy --prod --yes
```

## Post-deploy

- Verificar en la salida: `Build Completed` y/o `readyState: READY` / `status: ok`.
- Reportar URL de producción (`https://john-henry-oficial.vercel.app`), resultado del build y warnings relevantes.
- Tras cambiar env vars en Vercel, redesplegar con `--force` si hace falta que el build las tome.
